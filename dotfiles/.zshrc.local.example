# Copy to ~/.zshrc.local on each machine and put machine-specific things here:
# secrets, proxy/network env vars, optional toolchain sourcing, work-only configs.
# Sourced from ~/.zshrc if present, never committed.
